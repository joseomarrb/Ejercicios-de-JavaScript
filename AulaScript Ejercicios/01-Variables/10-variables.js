/**
 * Pues seguimos con los números. 
 * Escribe un script  que dado un número nos devuelva true si es un número par
 * y false si es un número impar. No usar condiconales.
 */

const numero = parseInt(prompt("Ingresa un número para saber si es par:"));

let resultado = alert(Number.isInteger(numero / 2))